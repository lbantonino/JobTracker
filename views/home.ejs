<%- include('partials/header'); -%>

  <% if (user) { %>
    <header>
      <!-- <div class="smoothie">
        <img src="<%= user.profilpicture %>" alt="">
      </div> -->
      <div class="headings">
        <h2>JOB TRACKER</h2>
        <h3>Bonjour <%= user.firstname %>
        </h3>
        <a href="/list-jobs" class="btn">View Jobs</a>
        <a href="/create-job" class="btn">Create a job</a>
      </div>
    </header>
    <% } else { %>
      <div class="headings">
        <a href="/login" class="btnlog">Login</a>
        <a href="/signup" class="btnlog sig">Signup</a>
      </div>
      <% } %>

        <%- include('partials/footer'); -%>